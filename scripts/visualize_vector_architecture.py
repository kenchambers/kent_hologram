#!/usr/bin/env python3
"""
Visualize the vector storage architecture and data flow.

Creates a visual representation of how information flows through
the different vector storage layers in the Hologram system.
"""

import sys
from pathlib import Path

# Add src to path
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))


def print_architecture_diagram():
    """Print ASCII diagram of vector storage architecture."""
    
    diagram = """
╔══════════════════════════════════════════════════════════════════════════════╗
║                     HOLOGRAM VECTOR STORAGE ARCHITECTURE                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                            INPUT PROCESSING LAYER                            │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                        User Input: "France's capital is Paris"
                                      │
                                      ▼
                    ┌─────────────────────────────────┐
                    │   Intent Classifier (Layer 3)    │
                    │  - Prototype vectors for intents │
                    │  - TEACHING, QUESTION, etc.      │
                    │  - In-memory, session-based      │
                    └─────────────────────────────────┘
                                      │
                                      ▼
                           Is it a TEACHING intent?
                                      │
                        ┌─────────────┴─────────────┐
                        │                           │
                       YES                         NO
                        │                           │
                        ▼                           ▼
┌───────────────────────────────────────┐  ┌──────────────────────────────────┐
│    FACT STORAGE (Layers 1a & 1b)     │  │  RESPONSE RETRIEVAL (Layer 6)    │
│                                       │  │                                  │
│  ┌─────────────────────────────────┐ │  │  ┌────────────────────────────┐ │
│  │  Layer 1a: In-Memory FactStore  │ │  │  │  Pattern Store (Layer 6)    │ │
│  │  - Fast query during session    │ │  │  │  - Pre-built responses      │ │
│  │  - Nested bindings: bind(S,P,O) │ │  │  │  - Intent-based retrieval   │ │
│  │  - Memory Trace underneath      │ │  │  └────────────────────────────┘ │
│  └─────────────────────────────────┘ │  │              │                  │
│                 │                     │  │              ▼                  │
│                 │ Persist             │  │  ┌────────────────────────────┐ │
│                 ▼                     │  │  │  Response Corpus (Layer 2) │ │
│  ┌─────────────────────────────────┐ │  │  │  - Learned responses        │ │
│  │ Layer 1b: ChromaDB (Persistent) │ │  │  │  - Context vectors          │ │
│  │  - Survives across sessions     │ │  │  │  - DISABLED in crew_trainer │ │
│  │  - Vector similarity search     │ │  │  └────────────────────────────┘ │
│  │  - ./data/crew_training_facts   │ │  │                                  │
│  └─────────────────────────────────┘ │  └──────────────────────────────────┘
│                                       │
│  ┌─────────────────────────────────┐ │
│  │ Layer 4: Entity Extractor       │ │
│  │  - Learns entities from facts   │ │
│  │  - Subject/Object vocabulary    │ │
│  │  - Entity recognition vectors   │ │
│  └─────────────────────────────────┘ │
└───────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         SESSION CONTEXT LAYER (Layer 5)                      │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Conversation Memory                                                    │ │
│  │  - Stores recent turns (user-bot pairs)                                │ │
│  │  - Recency-weighted context vectors                                    │ │
│  │  - Cleared at session end                                              │ │
│  │  - Used for context in response generation                             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW SUMMARY                                    │
└─────────────────────────────────────────────────────────────────────────────┘

1. USER INPUT → Intent Classifier (Layer 3)
   ↓
2. IF TEACHING:
   ├─ Extract S-P-O triple
   ├─ Store in In-Memory FactStore (Layer 1a) ─┐
   ├─ Persist to ChromaDB (Layer 1b) ──────────┤→ FACT LEARNED
   └─ Update Entity Extractor (Layer 4) ────────┘

3. IF QUESTION/STATEMENT:
   ├─ Query FactStore for relevant facts
   ├─ Retrieve from Pattern Store (Layer 6)
   ├─ Use Conversation Memory context (Layer 5)
   └─ Generate response

4. RESPONSE → Store in Conversation Memory (Layer 5)
   ↓
5. (Optional) Learn response in Corpus (Layer 2) - DISABLED by default

┌─────────────────────────────────────────────────────────────────────────────┐
│                         REDUNDANCY ANALYSIS                                  │
└─────────────────────────────────────────────────────────────────────────────┘

INTENTIONAL REDUNDANCY (Necessary):
✓ Layer 1a + 1b: In-Memory + Persistent storage
  → In-memory for speed, ChromaDB for persistence
  → This is REQUIRED for performance + durability

✓ Layer 4 learns from Layer 1: Entity Extractor from Facts
  → Entities extracted from fact subjects/objects
  → This is REQUIRED for entity recognition

NO REDUNDANCY:
✓ Layer 2 (Corpus) vs Layer 1 (Facts)
  → Corpus stores RESPONSES (conversational)
  → Facts store KNOWLEDGE (structured S-P-O)
  → Different purposes, no overlap

✓ Layer 3 (Intent) vs Layer 5 (Memory)
  → Intent stores CLASSIFICATION patterns
  → Memory stores SESSION context
  → Different purposes, no overlap

✓ Layer 6 (Patterns) vs Layer 2 (Corpus)
  → Patterns are TEMPLATES (pre-built)
  → Corpus is LEARNED responses (from training)
  → Different purposes, no overlap

POTENTIAL ISSUES:
⚠️  Duplicate facts within Layer 1b (ChromaDB)
  → Can occur if same fact is taught multiple times
  → Run: python scripts/deduplicate_facts.py

⚠️  Layer 2 enabled but unused
  → Wastes memory if not learning responses
  → crew_trainer correctly disables this
"""
    
    print(diagram)


def print_storage_comparison():
    """Print comparison table of storage layers."""
    
    table = """
┌─────────────────────────────────────────────────────────────────────────────┐
│                    STORAGE LAYER COMPARISON TABLE                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌────────┬──────────────────┬─────────────┬──────────────┬─────────────────────┐
│ Layer  │ Name             │ Persistence │ Size         │ Purpose             │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 1a     │ FactStore        │ In-memory   │ ~5KB/fact    │ Fast fact queries   │
│        │ (Memory Trace)   │ Session     │              │ during session      │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 1b     │ ChromaDB         │ Persistent  │ ~10KB/fact   │ Fact persistence    │
│        │                  │ On-disk     │              │ across sessions     │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 2      │ Response Corpus  │ In-memory   │ ~2KB/entry   │ Learned responses   │
│        │                  │ Session     │              │ (DISABLED default)  │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 3      │ Intent Prototype │ In-memory   │ ~100KB total │ Intent              │
│        │                  │ Session     │              │ classification      │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 4      │ Entity Extractor │ In-memory   │ ~1KB/entity  │ Entity recognition  │
│        │                  │ Session     │              │ from facts          │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 5      │ Conv. Memory     │ In-memory   │ ~5KB/turn    │ Session context     │
│        │                  │ Session     │              │ (recent turns)      │
├────────┼──────────────────┼─────────────┼──────────────┼─────────────────────┤
│ 6      │ Pattern Store    │ In-memory   │ ~1KB/pattern │ Pre-built response  │
│        │                  │ Session     │              │ templates           │
└────────┴──────────────────┴─────────────┴──────────────┴─────────────────────┘

MEMORY USAGE ESTIMATE (for typical training session):
  - 100 facts: ~500KB (in-memory) + ~1MB (ChromaDB)
  - 0 corpus entries: ~0KB (disabled)
  - 5 intent prototypes: ~100KB
  - 50 entities: ~50KB
  - 20 turns: ~100KB
  - 50 patterns: ~50KB
  TOTAL: ~1.8MB in-memory, ~1MB on-disk

DISK USAGE ESTIMATE (ChromaDB only):
  - 100 facts: ~1MB
  - 1000 facts: ~10MB
  - 10000 facts: ~100MB

OPTIMIZATION TIPS:
1. Disable Corpus (Layer 2) if not learning responses → DONE in crew_trainer ✓
2. Clear Conversation Memory (Layer 5) between rounds → session.start_session()
3. Run deduplication on ChromaDB periodically → scripts/deduplicate_facts.py
4. Limit in-memory fact cache if memory constrained → use ChromaDB only
"""
    
    print(table)


def main():
    """Main visualization routine."""
    print_architecture_diagram()
    print()
    print_storage_comparison()
    
    print("\n" + "="*80)
    print("📊 To analyze your current storage, run:")
    print("   python scripts/diagnose_vector_layers.py")
    print()
    print("🔧 To remove duplicate facts, run:")
    print("   python scripts/deduplicate_facts.py")
    print("="*80)


if __name__ == "__main__":
    main()
